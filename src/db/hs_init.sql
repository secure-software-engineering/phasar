create table spo_subject (name varchar unique notnull primary key);
create table spo_predicate (
    name varchar unique notnull,
    subject varchar notnull,
    primary key (name),
    
    
)
create table spo_predicate (name varchar unique not null primary key)
