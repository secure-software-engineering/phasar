name: Phasar CI

on:
  push:
    branches: [ master, development ]
  pull_request:
    branches: [ master, development ]

# TODO test in tree build?
# TODO test conan build
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-24.04, ubuntu-24.04-arm]
        compiler: [ [clang++-19, clang-19, "clang-19 libclang-rt-19-dev"] ]
        build: [ Debug, Release, DebugLibdeps, DebugCov ]
        include:
          - build: Debug
            cmake_build_type: Debug
            flags: -DPHASAR_ENABLE_SANITIZERS=ON -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          - build: Release
            cmake_build_type: Release
            flags: -DPHASAR_ENABLE_DYNAMIC_LOG=OFF -DPHASAR_BUILD_DYNLIB=ON -DPHASAR_ENABLE_SANITIZERS=ON -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          - build: DebugLibdeps
            cmake_build_type: Debug
            flags: -DPHASAR_DEBUG_LIBDEPS=ON -DBUILD_SHARED_LIBS=ON -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          - build: DebugCov
            cmake_build_type: Debug
            flags: -DCODE_COVERAGE=ON -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
            extra_dependencies: llvm-19 # For coverage
        exclude:
          - os: ubuntu-24.04-arm
            build: Debug
          - os: ubuntu-24.04-arm
            build: DebugLibdeps
          - os: ubuntu-24.04-arm
            build: DebugCov

    continue-on-error: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Phasar Dependencies
        shell: bash
        run: |
          ./utils/InstallAptDependencies.sh --noninteractive tzdata ${{ matrix.compiler[2] }} ${{ matrix.extra_dependencies }}


      - name: Download release build artifacts
        if: matrix.build == 'Release'
        shell: bash
        run: |
          name: ReleaseArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Download debug build artifacts
        if: matrix.build == 'Debug'
        uses: actions/upload-artifact@v4
        with:
          name: DebugArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Download debuglibdeps build artifacts
        if: matrix.build == 'DebugLibdeps'
        uses: actions/upload-artifact@v4
        with:
          name: DebugLibdepsArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Download debugcov build artifacts
        if: matrix.build == 'DebugCov'
        uses: actions/upload-artifact@v4
        with:
          name: DebugCovArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Building Phasar in ${{ matrix.build }} with ${{ matrix.compiler[0] }}
        env:
          CXX: ${{ matrix.compiler[0] }}
          CC: ${{ matrix.compiler[1] }}
        shell: bash
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=${{ matrix.cmake_build_type }} \
            -DBUILD_PHASAR_CLANG=OFF \
            -DPHASAR_USE_Z3=ON \
            ${{ matrix.flags }} \
            -G Ninja
          ninja -C build

      - name: Upload release build artifacts
        if: matrix.build == 'Release'
        uses: actions/upload-artifact@v4
        with:
          name: ReleaseArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Upload debug build artifacts
        if: matrix.build == 'Debug'
        uses: actions/upload-artifact@v4
        with:
          name: DebugArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Upload debuglibdeps build artifacts
        if: matrix.build == 'DebugLibdeps'
        uses: actions/upload-artifact@v4
        with:
          name: DebugLibdepsArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Upload debugcov build artifacts
        if: matrix.build == 'DebugCov'
        uses: actions/upload-artifact@v4
        with:
          name: DebugCovArtifact
          path: ~/.cache/ccache
          if-no-files-found: error
          retention-days: 7

      - name: Run Unittests
        shell: bash
        run: |
          cmake --build ./build --target check-phasar-unittests

      - name: Install PhASAR and Build Examples
        if: matrix.build == 'DebugLibdeps' # Circumvent conflicting ASAn flags
        env:
          CXX: ${{ matrix.compiler[0] }}
          CC: ${{ matrix.compiler[1] }}
        shell: bash
        run: |
          cmake -DCMAKE_INSTALL_PREFIX=./INSTALL -P ./build/cmake_install.cmake
          PHASAR_ROOT_DIR=$(pwd)
          cd ./examples/how-to
          cmake -S . -B build -Dphasar_ROOT="$PHASAR_ROOT_DIR/INSTALL"
          cmake --build ./build --target run_sample_programs

      - name: Check test coverage and generate HTML report
        if: matrix.build == 'DebugCov'
        shell: bash
        run: |
          cmake --build ./build --target ccov-all

      - name: Upload coverage HTML report artifact
        if: matrix.build == 'DebugCov'
        uses: actions/upload-artifact@v4
        with:
          name: CoverageReport
          path: ./build/ccov/all-merged/
          if-no-files-found: error
          retention-days: 7
